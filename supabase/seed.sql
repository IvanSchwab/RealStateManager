-- ============================================
-- Seed: Promote first user to admin
-- ============================================
-- Instructions for creating the first admin user:
-- 1. Register via the UI or create a user in Supabase Studio
--    (http://127.0.0.1:54323 → Authentication → Users → Add user)
-- 2. Run the query below replacing the email with your own:

-- UPDATE profiles SET role = 'admin' WHERE email = 'admin@example.com';
